# ============================================
# CONFIGURACIÓN DE BACKEND PARA HOSTING
# ============================================

# Configuración de Base de Datos en Hosting
DB_HOST=127.0.0.1
DB_USER=u361269377_appcrm
DB_PASSWORD=tu_password_de_bd_aqui
DB_NAME=u361269377_appcrm
DB_PORT=3306

# JWT Secret (CAMBIAR en producción a algo único y seguro)
JWT_SECRET=tu_secret_key_muy_segura_y_larga_aqui_minimo_32_caracteres

# JWT Expiration
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS (Dominio de tu app Flutter web)
ALLOWED_ORIGINS=https://tu-dominio.com,http://localhost

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 1. Reemplaza "tu_password_de_bd_aqui" con tu contraseña real de phpMyAdmin
# 2. Genera un JWT_SECRET seguro (puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# 3. En ALLOWED_ORIGINS, agrega el dominio donde alojarás tu app Flutter
# 4. Si usas un hosting compartido, el DB_HOST probablemente sea "localhost" o "127.0.0.1"
# 5. Este archivo NO debe subirse a Git (agrégalo a .gitignore)

# ============================================
# PARA GENERAR UN JWT_SECRET SEGURO:
# ============================================
# Ejecuta en terminal Node.js:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Copia el resultado y pégalo en JWT_SECRET
